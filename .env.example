# App
APP_NAME=Freshly API
APP_ENV=local
LOG_LEVEL=INFO

# Database (local default)
DATABASE_URL=sqlite:///./app.db
# Example for Supabase later (do not commit real values):
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/postgres?sslmode=require

# CORS (comma-separated, include your frontend)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Auth (Option A – FastAPI JWT)
JWT_SECRET=replace_me_in_local_only
ACCESS_TOKEN_EXPIRE_MINUTES=1440
JWT_ALG=HS256